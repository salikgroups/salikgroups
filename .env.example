# Optional — site URL is configured in src/config/seo.ts for production.
# Use these only if you later wire env-based metadata for preview deployments.

# NEXT_PUBLIC_SITE_URL=https://salikgroups.com
